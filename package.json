{
  "name": "api-generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon index.js",
    "start": "npm run generate-data && node index.js",
    "generate-data": "node generator-data.js"
  },
  "author": "taiducle",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3",
    "query-string": "^7.0.1"
  },
  "devDependencies": {
    "faker": "^5.5.3",
    "nodemon": "^2.0.12"
  }
}
